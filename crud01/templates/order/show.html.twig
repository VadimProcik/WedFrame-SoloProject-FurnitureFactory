{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>CutOutSheet</th>
                <td>{{ order.cutOutSheet }}</td>
            </tr>
            <tr>
                <th>AssemnlyDate</th>
                <td>{{ order.assemnlyDate }}</td>
            </tr>
            <tr>
                <th>FittingDate</th>
                <td>{{ order.fittingDate }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ order.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_index') }}">back to list</a>

    <a href="{{ path('app_order_edit', {'id': order.id}) }}">edit</a>

    {{ include('order/_delete_form.html.twig') }}
{% endblock %}
