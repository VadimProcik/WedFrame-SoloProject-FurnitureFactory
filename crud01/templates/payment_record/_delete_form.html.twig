<form method="post" action="{{ path('app_payment_record_delete', {'id': payment_record.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_record.id) }}">
    <button class="btn">Delete</button>
</form>
